import React from "react";

function Header() {

    return (
        <div className="header">
            <p className="logo">
                Where's Waldo?
            </p>
            <button type="button" className="leaderboard-button">See the leaderboard</button>
        </div>
    )
}

export default Header;